# Route ALSA clients to the clocked PulseAudio null sink selected by PULSE_SINK.
# A raw ALSA "type null" PCM consumes audio as fast as the CPU allows and makes
# playback-state assertions race against sound completion.
pcm.!default {
    type pulse
}

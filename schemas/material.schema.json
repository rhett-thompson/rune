{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "material.schema.json",
  "title": "Rune material",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "base_color": {
      "type": "array",
      "description": "RGBA color multiplied into the rendered model or primitive.",
      "items": { "type": "integer", "minimum": 0, "maximum": 255 },
      "minItems": 4,
      "maxItems": 4,
      "default": [255, 255, 255, 255]
    },
    "texture": {
      "type": "string",
      "description": "Project-relative diffuse/albedo texture path. Kept as an alias for albedo."
    },
    "albedo": {
      "type": "string",
      "description": "Project-relative diffuse/albedo texture path."
    },
    "normal": {
      "type": "string",
      "description": "Project-relative tangent-space normal map path."
    },
    "normal_scale": {
      "type": "number",
      "description": "Normal map strength multiplier from 0 to 4.",
      "minimum": 0,
      "maximum": 4,
      "default": 1
    },
    "emission": {
      "type": "string",
      "description": "Project-relative emission texture path. Alias for emission_texture."
    },
    "emission_texture": {
      "type": "string",
      "description": "Project-relative emission texture path."
    },
    "emission_color": {
      "type": "array",
      "description": "RGBA color multiplied into emission.",
      "items": { "type": "integer", "minimum": 0, "maximum": 255 },
      "minItems": 4,
      "maxItems": 4,
      "default": [255, 255, 255, 255]
    },
    "emission_energy": {
      "type": "number",
      "description": "Emission strength multiplier.",
      "minimum": 0,
      "default": 0
    },
    "orm_texture": {
      "type": "string",
      "description": "Project-relative packed occlusion/roughness/metallic texture path. R=AO, G=roughness, B=metallic."
    },
    "orm": {
      "type": "string",
      "description": "Alias for orm_texture."
    },
    "roughness_texture": {
      "type": "string",
      "description": "Project-relative roughness texture path. The red channel is sampled."
    },
    "metallic_texture": {
      "type": "string",
      "description": "Project-relative metallic texture path. The red channel is sampled."
    },
    "ao_texture": {
      "type": "string",
      "description": "Project-relative ambient occlusion texture path. The red channel darkens ambient light."
    },
    "ao_strength": {
      "type": "number",
      "description": "Ambient occlusion strength multiplier from 0 to 1.",
      "minimum": 0,
      "maximum": 1,
      "default": 1
    },
    "occlusion": {
      "type": "string",
      "description": "Alias for ao_texture."
    },
    "height_texture": {
      "type": "string",
      "description": "Project-relative height texture path reserved for custom/parallax material shaders. The r3d default material does not consume this yet."
    },
    "height": {
      "type": "string",
      "description": "Alias for height_texture."
    },
    "filter": {
      "type": "string",
      "description": "Sampling filter for material textures.",
      "enum": ["point", "bilinear", "trilinear", "anisotropic_4x", "anisotropic_8x", "anisotropic_16x"],
      "default": "anisotropic_8x"
    },
    "mipmaps": {
      "type": "boolean",
      "description": "Generate mipmaps before applying the texture filter.",
      "default": true
    },
    "lod_bias": {
      "type": "number",
      "description": "Positive mip LOD bias for noisy textures; higher values sample softer mip levels.",
      "minimum": 0,
      "maximum": 4,
      "default": 0
    },
    "lighting": {
      "type": "boolean",
      "description": "Whether the material participates in r3d lighting.",
      "default": false
    },
    "roughness": {
      "type": "number",
      "description": "Lit material roughness from 0 to 1.",
      "minimum": 0,
      "maximum": 1,
      "default": 0.5
    },
    "metallic": {
      "type": "number",
      "description": "Lit material metallic response from 0 to 1.",
      "minimum": 0,
      "maximum": 1,
      "default": 0
    },
    "specular": {
      "type": "number",
      "description": "Base reflectivity multiplier for non-metal materials from 0 to 1.",
      "minimum": 0,
      "maximum": 1,
      "default": 0.5
    },
    "alpha_cutoff": {
      "type": "number",
      "description": "Alpha cutoff threshold from 0 to 1.",
      "minimum": 0,
      "maximum": 1,
      "default": 0.01
    },
    "transparency": {
      "type": "string",
      "description": "Transparency rendering mode.",
      "enum": ["disabled", "prepass", "alpha"],
      "default": "disabled"
    },
    "blend": {
      "type": "string",
      "description": "Blend mode used when transparency is active.",
      "enum": ["mix", "additive", "multiply", "premultiplied_alpha"],
      "default": "mix"
    },
    "cull": {
      "type": "string",
      "description": "Face culling mode.",
      "enum": ["back", "front", "none"],
      "default": "back"
    },
    "height_scale": {
      "type": "number",
      "description": "Height/parallax scale reserved for custom material shaders. The r3d default material does not consume this yet.",
      "minimum": 0,
      "maximum": 0.2,
      "default": 0.03
    }
  },
  "additionalProperties": false
}
